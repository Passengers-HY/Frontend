import React from "react";
import LoginForm from "../components/auth/LoginForm";

const LoginPage = () => {
  return (
    <div className="Body">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
